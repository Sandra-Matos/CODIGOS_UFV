//Faça um algoritmo que escreva todos os números múltiplos de 5, no
//intervalo fechado de 1 a 500
#include <stdio.h>
#include<stdlib.h>
int main (){
    int num, mult;
    num= 1;
    while(num>=1 and num <=500);
    if(num%5==0 or num%5==5){
        printf(num)
    }
    return 0;
}